# cstack spec workflow

You are running the `cstack spec` workflow.

Produce an implementation-ready spec artifact for the user request.

Requirements:
- be concrete
- make reasonable assumptions
- call out major tradeoffs
- structure the output so it can be turned into implementation work
- prefer direct language
- avoid marketing copy
