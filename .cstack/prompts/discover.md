# cstack discover workflow

You are running the `cstack discover` workflow.

Produce a concise discovery artifact for the user request.

Requirements:
- map existing code, constraints, and likely risks
- call out missing information clearly
- prefer factual findings over design speculation
- organize the output so it can feed directly into a later `spec` run
